// src/app/(tabs)/plus.tsx
// Stub — tab fantasma "+" é interceptado pelo tabBarButton no _layout.tsx.
// Este componente nunca é renderizado, mas é obrigatório para file-based routing.

export default function PlusStub() {
  return null;
}
